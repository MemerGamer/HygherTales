# CurseForge API key (required). Set in .env only; see README for how to get a key.
# No quotes. Keys with $ characters work correctly (no escaping needed).
CURSEFORGE_API_KEY=

# Server port (default: 8787)
PORT=8787

# Hytale's CurseForge game ID (numeric only, not the slug "hytale"). Required for search and featured.
# Get it: curl -H "x-api-key: YOUR_KEY" "https://api.curseforge.com/v1/games?pageSize=50"
#         then set this to the "id" of the game with "slug": "hytale"
CURSEFORGE_GAME_ID=70216

# Optional: CORS allowlist, comma-separated (default: localhost dev origins)
# CORS_ORIGINS=http://localhost:1420,http://localhost:5173

# Optional: Per-IP rate limit (requests per minute, default: 60)
# RATE_LIMIT_PER_MIN=60

{
  "name": "hyghertales",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Run from apps/desktop or apps/proxy'",
    "dev:all": "concurrently --names proxy,desktop -c blue,green \"bun run --cwd apps/proxy dev\" \"bun run --cwd apps/desktop dev\"",
    "build": "bun run build --cwd packages/shared && bun run build --cwd apps/proxy && bun run build --cwd apps/desktop",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/bun": "latest",
    "concurrently": "^9.1.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  }
}
